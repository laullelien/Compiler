//Unary Minus Assignation et ConvFloat

{
 
    int a, b;
    float x, y;
    float result;

    a = 7;
    b = -3;
    x = -2.5;
    y = 1.5;

    result = ((-a + (b * -1)) * -x) / (-y + -1);

    println(result);
}

