
{
    // Test 9: Opérations avec des valeurs négatives et multiplication par une variable (conditions inversées)
    int i;
    int multiplier = 2;

    if (!((i = -4) * multiplier > (2 % 3.0) && ((-3 % (1 - 3) == 2)))) {
        println("le fou");
    }
}